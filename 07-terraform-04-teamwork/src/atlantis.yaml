version: 3
projects:
- dir: "."
  workspace: stage
  autoplan:
    when_modified: ["./modules/**/*.tf", "*.tf*"]
- dir: "."
  workspace: prod
  autoplan:
    when_modified: ["./modules/**/*.tf", "*.tf*"]